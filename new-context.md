Context for the agent: Provide this to your coding assistant to set the architectural constraints for how the frontend should be built for integration.System Architecture & Integration Directive: Mini-App / WebView ImplementationObjective: We are building the frontend surfaces for "e-Laya," a system designed to integrate into the existing eGovPH Super App. The architecture requires these surfaces to function as Mini-Apps rendered via WebView within the host application.  Technical Constraints & Implementation Details:Surface-Based Architecture: The system consists of six independent UIs (surfaces) rather than a single-page application (SPA). Each surface targets a specific user role (e.g., Family, Custody, Cases).  Host Environment: Assume the frontend will be loaded seamlessly inside an in-app browser/WebView within the eGovPH Super App shell. The UI must look and feel like native application screens.  Authentication Passing: We are utilizing eGov SSO. Expect the host app to pass down authentication tokens or session data to our WebView surface without requiring the user to log in again manually.  Tech Stack Alignment: While the MVP utilizes vanilla HTML/JS, structure your frontend component output (React/Next.js or Flutter Web) to be PWA-compliant and easily embeddable.  UI/UX Requirements: Ensure strict adherence to mobile responsiveness. Touch targets must be a minimum of 44×44px (WCAG 2.1 standard) to accommodate users on mobile devices interacting with the WebView.  Graceful Degradation: If the WebView loses connection or APIs fail, the interface must degrade gracefully using cached data (local storage/service workers) rather than showing a blank screen.  